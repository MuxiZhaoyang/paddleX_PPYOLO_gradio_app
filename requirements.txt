# PP-YOLOE+ 智能目标检测应用依赖库
# PaddleX 及其计算机视觉依赖
paddlex[cv]>=3.0.0

# Gradio Web界面框架
gradio>=4.0.0

# 图像处理库
opencv-python>=4.6.0
Pillow>=9.0.0

# 数值计算库
numpy>=1.21.0

# 深度学习框架 (PaddlePaddle会通过paddlex[cv]自动安装)
# paddlepaddle>=2.5.0  # 由paddlex[cv]自动安装，无需单独指定

# 如果在GPU环境下，可以使用GPU版本的PaddlePaddle
# paddlepaddle-gpu>=2.5.0  # GPU版本，与paddlepaddle互斥

# 系统依赖库（通常系统已安装，但在某些Docker环境中可能需要）
# fontconfig  # 字体配置（Linux系统）
# fonts-liberation  # 开源字体包 